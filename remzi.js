
let names = prompt('Введите имя пользователя');
let ages  = +prompt('Введите возраст пользователя');

let user = {
    name: names,
    age: ages,  
}

    for (let i = 1; i < 10; i++) {
        let names = prompt('Введите имя пользователя');
        let ages  = +prompt('Введите возраст пользователя');
        
        let user = {
        name: names,
        age: ages,  
            }

        console.log(user);
    }

console.log(user);